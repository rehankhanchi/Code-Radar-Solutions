#include <stdio.h>
int main() {
    char v;
    scanf("%c",&v);
    if (v>=0 && v<=9);
    printf("Digit")
    else if(v==a||e||i||o||u)
    printf("Vowel");
    else
    printf("Special character");

    return 0;
} 